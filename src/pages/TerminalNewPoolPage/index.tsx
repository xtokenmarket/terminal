import { PageHeader, PageWrapper } from "components";

const TerminalNewPoolPage = () => {
  return (
    <PageWrapper>
      <PageHeader headerTitle="Create new LM pool" backVisible />
    </PageWrapper>
  );
};

export default TerminalNewPoolPage;
